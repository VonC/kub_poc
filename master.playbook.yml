---
- hosts: master
  become_method: sudo
  gather_facts: False

  pre_tasks:
    - raw: test -e /usr/bin/python || (apt -y update && apt install -y python-minimal)
      become: true
    - setup:

  roles:
    - rkt
    - kubernetes
  

  # - name: etcd
  #   become: true
  #   apt: 
  #     name: etcd
  #     update_cache: yes
